# secrets-allowlist.txt - scoped suppressions for scripts/secrets-lint.sh.
#
# Format per line: <repo-relative path prefix>|<fixed string>|<reason>
# A finding is suppressed only when its file path starts with the prefix AND
# the matched line contains the fixed string. Keep entries minimal and
# justified; every entry is covered by tests/test_ci_gates.py, which also
# asserts each entry still matches a live line (no dead suppressions).
cli/modules/hornero_core/redact_test.v|api_secret='s3cr3t'|synthetic redaction-test fixture, not a credential
cli/modules/hornero_core/redact_test.v|api_key: abc123|synthetic redaction-test fixture, not a credential
