# Release checklist: v0.2.0-preview2

Release definition: `v0.2.0-preview2.yaml`. Check each box before
tagging. Unchecked boxes block the release.

## Composition

- [x] Manifest `hornero-0.2.0-preview2` pins the frozen `shell` and
  `config` release-candidate SHAs with commit subjects recorded.
  shell `1096cf7`, config `b73144a` (2026-09-15).
- [x] `manifests/v0.1.0-draft.yaml` is byte-identical to tag
  `v0.1.0-draft` (verified with `cmp` tag-vs-main, 2026-09-15).
- [x] `manifests/candidate` names `v0.2.0-preview2.yaml`.
- [x] `installer` and `iso` are present as `future` slots only, with
  no `sha` or `ref`.
- [x] Profiles `base`, `desktop`, and `developer` reference the
  Preview 2 manifest and ship only `pinned` or `local` components.
- [x] Profile extension chain (`developer` -> `desktop` -> `base`)
  resolves with no cycles.

## Validation

- [x] `python3 scripts/check-manifests.py` exits 0.
- [x] `python3 scripts/check-pins.py` exits 0 (candidate freshness).
- [x] `python3 -m pytest tests/ -q` passes (27/27).
- [x] `markdownlint` is clean on all new or touched Markdown files.
- [x] `./scripts/compose.sh --yes` is green as the `compose` job in
  `composition-ci` on the freeze commit.
- [x] `tests/vm/smoke.sh` is green on KVM (22/22 `VM-SMOKE-PASS`,
  2026-09-15, trailer below).

## Hygiene

- [x] Personal-data and secrets rescan shows no findings
  (`scripts/secrets-lint.sh` SECRETS-PASS, 2026-09-15).
- [x] No git submodules added (`git submodule status` is empty).
- [x] Commits are small, English-only, on the release branch.
  `main` is never pushed directly.

## Tag and build order

- [x] Branch is pushed; CI (`composition-ci`) is green.
- [ ] Tag `v0.2.0-preview2` created per `docs/RELEASE_PROCESS.md`
  from `main` after merge; tag history of `v0.1.0-draft` untouched.
- [x] Build order followed: `horneroctl`, then `config`, then
  `shell`; `installer` and `iso` slots recorded as skipped
  (`compose.sh` + VM smoke).

## Release notes (v0.2.0-preview2, Development Preview 2)

Frozen 2026-09-15: shell `1096cf7b5f7cb6a3eec7562b4c46512cb25fc710`,
config `b73144a84fc0d3ec8b59669f3ef8996f9e7d1184`.

```text
VM-SMOKE-PASS: ALL GREEN (work dir: /home/ulisesjcf/.local/share/hornero/vm/work)
```

22/22 guest checks: materialize, config validate/show, 15-entry theme
manifest, shipped-script syntax, offline firewall, dark/light/pampa
trio via horneroctl.

Known follow-ups (not blocking): per-package AUR version truth +
trigger narrowing before first classic publishes; dedicated AUR SSH
key rotation (human action); media-base publish automation.
