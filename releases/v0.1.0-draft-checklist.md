# Release checklist: v0.1.0-draft

Release definition: `v0.1.0-draft.yaml`. Check each box before
tagging. Unchecked boxes block the release.

## Composition

- [x] Manifest `hornero-0.1.0-draft` pins current `shell` and `config`
  `main` SHAs with commit subjects recorded.
- [x] `installer` and `iso` are present as `future` slots only, with
  no `sha` or `ref`.
- [x] Profiles `base`, `desktop`, and `developer` reference the
  manifest and ship only `pinned` or `local` components.
- [x] Profile extension chain (`developer` -> `desktop` -> `base`)
  resolves with no cycles.

## Validation

- [x] `python3 scripts/check-manifests.py` exits 0.
- [x] `python3 -m pytest tests/ -q` passes.
- [x] `markdownlint` is clean on all new or touched Markdown files.
- [x] Existing `horneroctl-ci` workflow file is untouched.
- [x] `./scripts/compose.sh --yes` is green locally and as the `compose`
  job in `composition-ci` (10 gates: pins, checkouts, horneroctl build,
  materialize, paths/validate/show).
- [x] `tests/vm/smoke.sh` is green on KVM (14/14 `VM-SMOKE-PASS`,
  2026-09-13): guest materialize + validate + 12-entry manifest +
  shell syntax. Trailer pasted in the release notes below.

## Hygiene

- [x] Personal-data and secrets rescan shows no findings
  (names, emails, hosts, SSIDs, tokens, keys).
- [x] No git submodules added (`git submodule status` is empty).
- [x] Commits are small, English-only, on the release branch.
  `main` is never pushed directly.

## Tag and build order

- [ ] Branch is pushed; CI (`composition-ci`) is green.
  Branch `docs/preview0-catchup` pushed 2026-09-13; CI status
  still pending at checklist time.
- [ ] Tag `v0.1.0-draft` created per `docs/RELEASE_PROCESS.md`.
  Not created: tag lands after this branch merges and CI is green
  on `main`.
- [x] Build order followed: `horneroctl`, then `config`, then
  `shell`; `installer` and `iso` slots recorded as skipped.
  Executed by `scripts/compose.sh` (host) and `tests/vm/smoke.sh`
  (KVM guest) on 2026-09-13; slots remain `future` in the manifest.

## Release notes (v0.1.0-draft, Development Preview 0)

Path contract (`docs/PATH_CONTRACT.md`) landed and is implemented in
all three component repos (hornero #15, shell #16, config #7):
canonical `hornero/*` writes with read-only `dots/*` fallbacks.
`horneroctl` batch-2 delegation ships `config default-apps list`,
`config materialize`, and `config gui` (hornero #17). Executable
composition caught and fixed one real bug pre-release (theme-manifest
object shape, hornero #18). VM evidence 2026-09-13:

```text
VM-SMOKE-PASS: ALL GREEN (work dir: $HOME/hx-vm/smoke-test)
```

Known follow-ups (not blocking): one-shot `dots/*` to `hornero/*`
migrator (owner TBD), fallback-removal window (date TBD),
`$XDG_CONFIG_HOME/quickshell` namespace decision, shell factory
`shell.json` default, graphical quickshell smoke (no GPU/seat in CI).
